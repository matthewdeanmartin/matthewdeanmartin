<!DOCTYPE html>
<html lang="{{ config.languages.default }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ identity.name }} - {{ identity.tagline }}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.min.css">
    <style>
        /* Minimal overrides for the profile layout */
        header.container { padding-bottom: 0; }
        footer { margin-top: 3rem; text-align: center; opacity: 0.7; }
        .project-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1rem; }
    </style>
</head>
<body>
    <nav class="container">
        <ul>
            <li><strong>{{ identity.name }}</strong></li>
        </ul>
        <ul>
            <li><a href="index.html">Home</a></li>
            <li><a href="projects.html">Projects</a></li>
            <li><a href="apis/openapi.yaml">API</a></li>
            <li><a href="{{ config.identity.profiles[0].url }}" role="button" class="outline">Connect</a></li>
        </ul>
    </nav>

    <main class="container">
        {% block body %}{% endblock %}
    </main>

    <footer class="container">
        <hr>
        <small>
            Generated by <a href="https://github.com/example/readme-cms">README CMS</a><br>
            Mode: {{ mode.current }} | Updated: {{ identity.generated_at }}
        </small>
    </footer>
</body>
</html>